# 本地云开发凭证模板
#
# 用法：把本文件复制为同目录下的 .env.local，然后填入真实值。
#   PowerShell:  Copy-Item .env.local.example .env.local
#
# .env.local 已被 .gitignore 覆盖，不会进仓库；也请不要贴进聊天记录或截图。
# CI（GitHub Actions）不使用本文件，走 GitHub Secrets 注入的环境变量。

TCB_ENV_ID=你的云开发环境ID
TCB_SECRET_ID=你的腾讯云SecretId
TCB_SECRET_KEY=你的腾讯云SecretKey
